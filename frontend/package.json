{
  "name": "wrist-assistant-panel",
  "version": "0.1.0",
  "private": true,
  "description": "Home Assistant sidebar panel for Wrist Assistant custom complications",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "esbuild": "^0.28.2",
    "lit": "^3.3.3",
    "svgo": "^3.3.5",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
